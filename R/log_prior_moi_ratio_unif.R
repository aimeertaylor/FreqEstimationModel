#'@title Log prior MOI ratio
#'@name log_prior_moi_ratio_unif
#'@description Function to calculate the log of the prior MOI ratio when the MOI prior distribution is a discrete uniform distribution.
#' Note that the normalising constants due to truncation cancel.
#'@export
log_prior_moi_ratio_unif <- function(moi_proposed,
                                     moi_current,
                                     moi_hyperparameter,
                                     moi_size_hyperparameter){
  log_prior_moi_ratio <- 0

  return(log_prior_moi_ratio)
}
